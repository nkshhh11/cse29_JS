// javascript can change all the html elements in the page
//javascript can change all the html attributes in the page 
//javascrript can change all the css styles in the page
// javascript can removee existing html elemments and attributes 
//javascript can add new html elements and attributes.
//javascript can react to all existing html events in the page



// const x =document.getElementById("p1");

// // console.log(x);
// x.innerHTML="<b>Data structure</b>";

// const result = document.getElementsByClassName("p1");

// console.log(result[0]); 
// result[0].innerHTML = "Hello";

    // const x=document.getElementById("p2").getAttribute("class");
    // document.getElementById("d").innerHTML=x;
        

// click       on click event is used to trigger a function when an element is clicked.
// mouseover    on mouseover event is used to trigger a function when the mouse pointer is moved over an element.
// mouseout     on mouseout event is used to trigger a function when the mouse pointer is moved out of an element.  

//JSON



document.body.style.backgroundcolor = 
"#" + Math.floor(Math.random())*16777215.tostring(16);
